name: Release Claude Plugin

on:
  pull_request_target:
    types: [closed]
    branches: [main]

permissions:
  contents: write

concurrency:
  group: clone-plugin-release-main
  cancel-in-progress: false

jobs:
  release:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest

    steps:
      - name: Check out main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Bump minor version
        run: node scripts/bump-plugin-version.mjs --part minor

      - name: Test release candidate
        run: npm test

      - name: Commit and tag release
        run: |
          VERSION="$(node -p "JSON.parse(require('fs').readFileSync('.claude-plugin/plugin.json', 'utf8')).version")"
          TAG="clone-plugin-v${VERSION}"

          if git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "Tag $TAG already exists"
            exit 1
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .claude-plugin/plugin.json hooks/stop-hook.mjs hooks/ask-user-question-hook.mjs README.md
          git commit -m "chore: release ${TAG}"
          git tag -a "$TAG" -m "$TAG"
          git push origin HEAD:main "$TAG"
